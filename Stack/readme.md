### Stack concept 
`Stack class in Java extends the Vector class, meaning that it inherits all the methods and properties of Vector. Since Stack is a subclass of Vector`
`Stack it give output as Vector in java because it is subset of vector in java also indexing is start from the zero`
